# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=expertco_db
DB_POOL_SIZE=5

# Server Configuration
PORT=5000
FLASK_DEBUG=False
LOG_LEVEL=INFO

# CORS Configuration (comma-separated list of allowed origins)
# For production, specify your actual domain(s)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Instructions:
# 1. Copy this file to .env in the backend directory
# 2. Update the values with your actual MySQL credentials
# 3. For production, set ALLOWED_ORIGINS to your actual domain(s)
# 4. Never commit the .env file to version control
